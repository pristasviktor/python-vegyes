# Function Definitions

# Check if a given string is a palindrome.
def is_palindrome(s: str):
    return False

# Count the number of vowels in a given string.
def count_vowels(s: str):
    return 0

# Reverse the order of words in a given sentence.
def reverse_words(sentence: str):
    return ""

# Compress a string by replacing sequences of the same character with that character followed by the count.
def compress_string(s: str):
    return ""

# Check if two strings are anagrams of each other.
def are_anagrams(s1: str, s2: str):
    return False